version: '3.8'

services:
  deepseek-ocr:
    build:
      context: .
      dockerfile: Dockerfile.arm64.gpu
      platforms:
        - linux/arm64
    image: deepseek-ocr-webui:arm64-gpu
    container_name: deepseek-ocr-webui-arm64-gpu
    ports:
      - "8001:8001"
    shm_size: 8g
    restart: unless-stopped
    # Note: MPS only works on macOS, not in Linux containers
    # The service will auto-detect and use CPU backend in Linux containers
    # For Apple Silicon Mac, use native Python environment instead of Docker
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5m

